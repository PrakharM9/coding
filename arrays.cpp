#include<bits/stdc++.h>
using namespace std;
int main(){
    int num,value,condition=1,count=0;
    cin>>num;
    int array[num];
    int array2[num];
    for(int i=0;i<num;i++){
        value=array[i];
        for(int j=0;j<count;j++){
            if(array2[j]==value){
                condition
            }
        }
    }
    return 0;
}